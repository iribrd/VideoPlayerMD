@model IEnumerable<videoSitecore2.Models.Videoview>

@{
    ViewData["Title"] = "Index";
}

    <div >
        <h1>
            لیست ویدئوها
        </h1>
    </div>
   

<!--<p>
    <a asp-action="Create">Create New</a>
</p> -->

<form asp-controller="Videos" asp-action="Index" method="get">
    <p>
        Title: <input type="text" name="SearchString">
        <input type="submit" value="Filter" />
    </p>
</form>

<table class="table" >
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {

            <tr>
                <td>

                    @Html.DisplayFor(model => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Description)

                </td>
                <td>
                    @Html.DisplayFor(model => item.Title)

                </td>
                <td>
                    <!--  <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> | -->
                    <!-- <a asp-action="Details" asp-route-id="@item.Id">Details</a> | -->
                    <!--  <a asp-action="Delete" asp-route-id="@item.Id">Delete</a> | -->
                    <!--<a asp-action="Play" asp-route-id="@item.Id">Play</a> | -->
                    <!--<a asp-action="Display" asp-route-id="@item.Id">Display</a> | -->
                    <!-- <a asp-action="playVideo" asp-route-id="@item.Id">playVideo</a> |  -->
                    <!-- <a asp-action="startTimeEvent2" asp-route-id="@item.Id" asp-route-metadataTitleId="14">startTimeEvent2</a> | -->
                    <!--  <a asp-action="startTimeEvent" asp-route-id="@item.Id">startTimeEvent</a> -->
                    <a asp-action="player" asp-route-id="@item.Id"><img src="~/images/icon.png" alt="نمایش ویدئو"></a>

                </td>
                            </tr>
                        }
                    </tbody>
                </table>
